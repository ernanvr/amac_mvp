# Instruction to run a local server

1. Clone repository
2. Run command `npm install`
3. Run command `npm run dev` and navigate to localhost route
4. User "usuario@amac.edu.sv" and password "password"
